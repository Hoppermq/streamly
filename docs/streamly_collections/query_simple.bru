meta {
  name: query_simple
  type: http
  seq: 4
}

post {
  url: {{query-service-url}}/v1/queries
  body: json
  auth: none
}

body:json {
  {
    "select": ["event_name", "user_id", "timestamp"],
    "from": "events",
    "timeRange": {
      "start": "now-1h",
      "end": "now"
    }
  }
}
